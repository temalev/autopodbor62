# SEO‑план развития сайта Автоподбор 62

_Обновлён 2026‑07‑27 по данным Яндекс.Вебмастера, Метрики и проверки живого прода. Выполненные пункты убраны — в файле только то, что осталось сделать._

---

## Где мы сейчас (база для замера прогресса)

| Показатель | Значение на 2026‑07‑27 |
|---|---|
| ИКС (SQI) | **0** |
| Страниц в поиске | 10 из 10 — индексация полная |
| Показы / клики в Яндексе (30 дн) | ~85 / 8 |
| Визиты (Метрика, 30 дн) | 32, посетителей 28 |
| Из поиска | 18 (Google 7, Яндекс 5, Яндекс.Карты 1) |
| Визитов из Рязани | **2** (Ростов 7, Москва 5) |
| Входов на страницы услуг | ≈1 из 32 — 29 визитов на главную |

**Позиции по коммерческим запросам** (Вебмастер, 30 дн):

| Запрос | Позиция | Клики |
|---|---|---|
| автоподбор 62 *(бренд)* | 3.3 | 4 |
| автоподбор 62 рязань *(бренд)* | 4.2 | 3 |
| автоподбор в рязани | 15.6 | 0 |
| подбор авто рязань | 17.0 | 0 |
| автоподбор рязань | 18.5 | 0 |
| автоподборщик рязань | 19.0 | 0 |
| подбор авто в рязани | 21.0 | 0 |
| пригнать авто из китая под ключ в рязани | 9–10 | 0 |

**Вывод:** бренд в топ‑3, вся коммерция на 2–3 странице. Страницы услуг проиндексированы, но входов не получают. Техническая база (meta, JSON‑LD, canonical, sitemap, перелинковка) сделана — упёрлись в контент, коммерческие факторы и полное отсутствие внешних сигналов.

**Запросы, показывающие неудовлетворённый спрос:** «рязань сколько стоит автоподбор», «сколько сейчас стоит автоподборка под ключ», «autopodbor_62 ул. грибоедова, 8б цены» — цен на сайте нет ни одной.

---

## ПРИОРИТЕТ 1. Дожать позиции 12–21 → топ‑10

Самая дешёвая работа с прямым влиянием на трафик. Запросы уже на второй странице — их надо дотолкать.

### ☐ 1.1 Блок цен на каждой странице услуги

Прайса нет нигде, при этом ценовой спрос виден прямо в запросах. Для Яндекса цена — прямой коммерческий фактор ранжирования.

- Блок «Стоимость» с вилкой («от X ₽») на всех 6 страницах услуг.
- Добавить `offers` / `PriceSpecification` в `serviceJsonLd()` — [app/utils/schema.ts:105](app/utils/schema.ts#L105).
- На главную — сводную таблицу цен по услугам.

### ☐ 1.2 Нарастить объём текста на страницах услуг

Сейчас: `/o-nas` — 223 слова, `/proverka` — 344, `/podbor` — 370, `/registraciya-gibdd` — 397, `/vykup` — 439, `/import` — 444, главная — 858. В топ‑10 Рязани по этим запросам объём в 3–5 раз больше.

Цель — 1000–1500 слов на страницу услуги. Чем добивать: развёрнутый процесс работы, что входит и что не входит, частые проблемы и как их ловим, примеры из практики с фото, сравнение вариантов услуги.

### ☐ 1.3 FAQ на каждую страницу услуги

`faqPageJsonLd()` используется только на главной.

- Раскомментировать 2 вопроса на главной — [index.vue:813](app/pages/index.vue#L813) («Что такое Автоподбор62», «Какие автомобили подбираете») и добавить их же в `faqPageJsonLd()`.
- По 3–5 вопросов на каждую страницу услуги + `faqPageJsonLd()` из [schema.ts](app/utils/schema.ts).

### ☐ 1.4 Вернуть `/dkp` в навигацию

Ссылки закомментированы в [Header.vue:84](app/components/Header.vue#L84) и [Footer.vue:51](app/components/Footer.vue#L51). Страница живая и в sitemap, но из навигации на неё нет ни одной ссылки — то есть внутреннего веса она не получает.

Это единственный на сайте инструмент под массовый информационный спрос («договор купли-продажи автомобиля скачать / заполнить онлайн»). Открыть.

### ☐ 1.5 Отдельная страница «Контакты»

Для локального бизнеса — одна из главных посадочных. Сейчас её роль тянет `/o-nas` на 223 словах. Нужны: карта, схема проезда, режим работы, телефон, мессенджеры, фото офиса на Грибоедова 8Б.

---

## ПРИОРИТЕТ 2. ИКС = 0 — это потолок для всей коммерции

Ноль означает, что у Яндекса нет ни ссылочных, ни поведенческих сигналов о компании. Пока так — коммерческий топ недостижим, сколько бы ни правили страницы. Кода тут нет, работа руками.

**Что уже есть:** карточка в Яндекс Бизнесе заведена и сайт в ней указан. Вебмастер это подтверждает — диагностика `NOT_IN_SPRAV` отсутствует, `NO_REGIONS` тоже, то есть **регион сайта присвоен**. Это закрывает геопривязку, но на ИКС почти не влияет: привязка карточки — не ссылочный сигнал.

**В чём проблема:** карточка подключена, но как источник трафика мертва — **1 визит с Яндекс.Карт за 30 дней**. При том что по запросу «автоподбор рязань» колдунщик Карт стоит в выдаче выше органики. То есть точка входа есть, но по ней не проходят — карточка проигрывает конкурентам внутри самих Карт.

- ☐ **Отзывы на Яндекс.Картах** — главный рычаг ранжирования внутри Карт. Сейчас в карточке **4,8 / 26 отзывов / 55 оценок** — база приличная, но для топа Карт по «автоподбор рязань» мало: у активных конкурентов счёт идёт на сотни. Настроить постоянный сбор: QR/ссылка после сделки, просьба при выдаче авто. Цель — +15–20 отзывов за квартал.
- ☐ **Проверить свежесть отзывов** — в первой выдаче виджета пять отзывов, самый свежий от 23.11.2025, за 2026 год нет ни одного. Порядок там не хронологический, так что это не приговор — но открыть карточку и посмотреть дату последнего отзыва стоит. Яндекс учитывает свежесть, и «мёртвая» лента тянет карточку вниз сильнее, чем небольшое общее число.
- ☐ **Аудит заполнения карточки** — фото офиса и работ, полный перечень услуг **с ценами** (те же, что появятся на сайте по п. 1.1), режим работы, категории. Незаполненные поля прямо опускают карточку в ранжировании Карт.
- ☐ **Активность в карточке** — посты, обновление фото, ответы на все отзывы. Яндекс учитывает свежесть, «поставил и забыл» не работает.
- ☐ **2ГИС, Google Business Profile** — завести и заполнить.
- ☐ **Ссылки на сайт** из профиля Авито, Telegram‑канала, локальных каталогов Рязани, профильных площадок автоподборщиков.
- ☐ **NAP‑консистентность**: одинаковые название, адрес и телефон везде. Заодно проверить e‑mail `info@autopodbor62.ru` в [schema.ts:32](app/utils/schema.ts#L32) — домен не совпадает с сайтом `автоподбор62.рф`, убедиться, что почта живая.

---

## ПРИОРИТЕТ 3. Технические правки

### ☐ 3.1 Canonical указывают на редиректы (все 9 внутренних страниц)

GitHub Pages отдаёт `301: /podbor → /podbor/`, а canonical, `og:url` и sitemap указывают версию **без** слэша:

```
canonical    → https://…/podbor    (301 →)
реальный URL → https://…/podbor/
```

Метрика подтверждает: входы фиксируются на `/o-nas/`, `/import/`. Получается цепочка «canonical → редирект» на всём сайте.

Фикс: включить `trailingSlash: true` в конфиге `@nuxtjs/sitemap` в [nuxt.config.ts:15](nuxt.config.ts#L15) и привести `PAGE_URL` во всех страницах к форме со слэшем (это же значение идёт в canonical, `og:url` и `@id` в JSON‑LD).

### ☐ 3.2 Переезд с GitHub Pages на российский хостинг

Сайт отдаётся GitHub Pages через франкфуртский edge (`x-served-by: cache-fra`), TTFB из России ~0.96 с. Для рязанского локального бизнеса это и скорость (фактор ранжирования), и риск деградации доступа.

Куда: Timeweb / Selectel / VK Cloud + российский CDN. Билд статический, переезд — это перенос `.output/public` и правка [.github/workflows/deploy.yml](.github/workflows/deploy.yml) на деплой по SSH/rsync вместо `peaceiris/actions-gh-pages`.

Побочный бонус: на своём хостинге можно настроить кэш‑заголовки (сейчас `cache-control: max-age=600` на всё, включая хэшированные ассеты `_nuxt/*`, которым можно ставить год).

### ☐ 3.3 Self-host шрифтов

Два render‑blocking запроса к `fonts.googleapis.com` — [nuxt.config.ts:62‑65](nuxt.config.ts#L62). Домен в РФ работает нестабильно и медленно, а это критический путь рендера.

Скачать Inter (нужные веса 400/500/600/700/800) и Material Symbols в `public/fonts/`, подключить через `@font-face` с `font-display: swap`, убрать `preconnect` и оба `<link rel="stylesheet">`.

### ☐ 3.4 Картинки услуг в WebP + ресайз логотипа

Висит с мая, остаётся главным замечанием по Core Web Vitals.

| Файл | Размер | Вес |
|---|---|---|
| `public/images/services/1.jpeg` | 1024×1024 | **784 КБ** |
| `public/images/services/2.jpeg` | 1024×1024 | **704 КБ** |
| `public/images/services/3.jpeg` | 1024×1024 | **356 КБ** |
| `public/images/logo.png` | **1684×1684** | 128 КБ (в шапке отображается 44×44) |

Остальное в порядке: `bg.webp` 64 КБ, `korea.webp` 108 КБ, `team/*` ~90 КБ, `import/*` под `loading="lazy"`.

Шаги:

1. Конвертировать:
   ```bash
   cd public/images/services
   for f in *.jpeg; do cwebp -q 82 "$f" -o "${f%.jpeg}.webp"; done
   rm *.jpeg
   ```
2. Заменить `.jpeg` → `.webp` в коде (в каждом файле два места — `PAGE_OG_IMAGE` и hero `<img>`):
   - [podbor.vue:35](app/pages/podbor.vue#L35) и [:147](app/pages/podbor.vue#L147)
   - [proverka.vue:35](app/pages/proverka.vue#L35) и [:148](app/pages/proverka.vue#L148)
   - [vykup.vue:35](app/pages/vykup.vue#L35) и [:161](app/pages/vykup.vue#L161)
   - [proverka-kuzova.vue:35](app/pages/proverka-kuzova.vue#L35) и [:163](app/pages/proverka-kuzova.vue#L163) — там `3.jpeg` для OG и `2.jpeg` для hero, заменить оба
   - [registraciya-gibdd.vue:35](app/pages/registraciya-gibdd.vue#L35) и [:161](app/pages/registraciya-gibdd.vue#L161)
3. Ресайзить `logo.png` до 256×256 (или сделать SVG). Он же используется в JSON‑LD как `logo`/`image` — [schema.ts:29](app/utils/schema.ts#L29).
4. Проверить: `npx nuxt generate`, DevTools → Network → Img, убедиться что отдаются `.webp`.

### ☐ 3.5 Большая иконка сайта (единственная активная рекомендация Вебмастера)

`BIG_FAVICON_ABSENT` — Яндексу нужен favicon ≥120×120 (SVG или PNG), сейчас только `.ico`. Влияет на иконку в сниппете выдачи. 10 минут работы, виден результат в SERP.

Положить `public/favicon.svg` (или `icon-192.png`) и добавить `<link rel="icon" type="image/svg+xml">` в [nuxt.config.ts:66](nuxt.config.ts#L66).

### ☐ 3.6 Мелочи одним коммитом

- Второй `<h1>` на `/dkp` — [dkp.vue:438](app/pages/dkp.vue#L438) (печатный шаблон, скрыт стилями, но в DOM). Сделать `<h2>` или `<div>`.
- `og:image` + `twitter:image` на `/privacy-policy` — [privacy-policy.vue:11](app/pages/privacy-policy.vue#L11), подставить `${SITE_URL}/images/bg.webp`.
- `image` не передаётся в `serviceJsonLd()` на главной — [index.vue:37](app/pages/index.vue#L37), подставляется квадратный логотип 1684×1684. Передать `bg.webp`.
- 4 `<img>` без `alt` в блоке отзывов — [index.vue:713](app/pages/index.vue#L713), 752, 762, 785.
- Глобальный `{ name: 'robots', content: 'index, follow' }` в [nuxt.config.ts](nuxt.config.ts) (на проде meta robots нет вообще).
- `{ name: 'theme-color', content: '#0a0a0a' }` и `{ name: 'format-detection', content: 'telephone=no' }`.
- `Clean-param` в [public/robots.txt](public/robots.txt) — Яндекс иначе считает `/?utm_source=…` отдельной страницей:
  ```
  Clean-param: utm_source&utm_medium&utm_campaign&utm_term&utm_content&yclid&gclid&fbclid&_openstat&from
  ```
- Урезать длинные title (Google режет ~60 символов): `/` — 80, `/dkp` — 85, `/vykup` — 85. Убрать хвост `| Автоподбор 62`.
- Урезать description до 150–160: `/` — 219, `/dkp` — 184, `/registraciya-gibdd` — 176, `/o-nas` — 173, `/podbor` — 165.
- JSON‑LD `WebSite` в [app/app.vue](app/app.vue) рядом с `LocalBusiness` — для @id‑связок.
- Удалить мёртвую зависимость `@element-plus/icons-vue` из [package.json:13](package.json#L13) — в коде не используется.
- `.DS_Store` в `public/` и `public/images/` попадают в билд — добавить в [.gitignore](.gitignore) и удалить из репо.

---

## ПРИОРИТЕТ 4. Рост на 3–6 месяцев

### ☐ 4.1 Блог / раздел «Полезное»

Главный способ поднять ИКС и собрать информационный трафик. `/blog/` или `/poleznoe/`, 2–4 материала в месяц по 3–5 тысяч знаков, в каждом CTA на услугу.

Темы под реальный спрос: «как проверить авто перед покупкой», «признаки скрученного пробега», «постановка на учёт в 2026», «пошлины на авто из Китая в 2026», разборы кейсов с фото.

### ☐ 4.2 Кластер страниц по импорту

Уже ловим позиции 9–10 по «пригнать авто из китая под ключ в рязани» при слабой локальной конкуренции — самое перспективное направление. Отдельные страницы под Корею и Китай, калькулятор пошлин, сроки и этапы доставки, галерея пригнанных авто.

### ☐ 4.3 Отзывы на страницах услуг

Виджет Яндекс.Карт стоит на главной ([YandexReviews.vue](app/components/YandexReviews.vue)). Подключить его же на страницы услуг — компонент переиспользуемый, принимает `heading`/`subheading`. Учитывать: контент виджета в iframe, в индекс не идёт и объём текста страницы не увеличивает (это к п. 1.2), работает только на доверие и сбор новых отзывов.

### ☐ 4.4 Региональные посадочные

`/podbor-skopin/`, `/podbor-kasimov/` и т.п. — **только после того, как Рязань выйдет в топ‑10**. Раньше смысла нет.

---

## Порядок работ

1. Приоритет 1 целиком — H1, цены, FAQ, объём текста, `/dkp` в меню. Это то, что двинет позиции 15→топ‑10.
2. Приоритет 2 параллельно — руками, без кода: Яндекс Бизнес, Карты, отзывы, каталоги.
3. Один технический коммит: 3.1 + 3.5 + 3.6.
4. Второй технический коммит: 3.3 + 3.4 (скорость).
5. Переезд с GitHub Pages (3.2).
6. Блог (4.1) — регулярно, начиная как можно раньше: эффект накопительный.

## Контрольные показатели

Замеряем через 2–3 месяца против базы вверху файла:

- «автоподбор в рязани», «подбор авто рязань» — из позиций 15–21 в **топ‑10**.
- ИКС — с 0 до **≥10**.
- Визиты из Рязани — с 2 до **20+** в месяц.
- Входы на страницы услуг — с ~1 до **30%** от всех визитов.
- Клики из Яндекса — с 8 до **40+** в месяц.

Инструменты замера: `node ~/.claude/skills/yandex-webmaster/webmaster.mjs --report all --days 30` и `node ~/.claude/skills/yandex-metrika/metrika.mjs --report all --days 30`. Снапшоты складываются в [.seo-snapshots/](.seo-snapshots/).

## Что прогонять после каждого деплоя

- https://pagespeed.web.dev/ — Core Web Vitals на главной и одной услуге (после 3.3 и 3.4 LCP должен заметно упасть).
- https://webmaster.yandex.ru/tools/microtest/ и https://search.google.com/test/rich-results — валидность JSON‑LD.
- Переобход в Вебмастере по списку из [routes-for-webmaster.txt](routes-for-webmaster.txt).
- Раз в неделю — отчёт «Запросы» в Вебмастере: где выросли позиции, где показы есть, а кликов нет (значит, переписать title/description).
