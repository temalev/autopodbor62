# SEO‑план развития сайта Автоподбор 62

_Обновлён 2026‑09‑01 по данным Яндекс.Вебмастера, Метрики, разбора конкурентов и аудита кода. Статусы проверены по факту, а не по памяти: выполненные пункты убраны, в файле только то, что осталось сделать._

---

## Что произошло с прошлой ревизии (27.07 → 30.08)

**Две аварии, обе разобраны и закрыты.**

4 августа робот Яндекса получил главную и `/import/` в виде страницы ошибки: не догрузился JS‑чанк, Nuxt показал `error.vue`, а она выставляла `noindex`. 7 августа обе страницы вылетели из индекса, поисковый трафик обнулился. Причину воспроизвели блокировкой чанка — картина совпала точно. Починено: `noindex` теперь ставится только на 404, а сорванная загрузка чанка чинится одной перезагрузкой (`app/plugins/chunk-reload.client.ts`). К 12 августа все 10 страниц вернулись в индекс сами.

**DNS‑авария из эпизодической стала постоянной.** 11–12 и 19 августа домен не резолвился через Google DNS; с 23 августа у сайта висит **FATAL `DNS_ERROR` в Вебмастере**, на 01.09 он держится десятый день. Причина установлена точно: Google отдаёт SERVFAIL с расширенным кодом **EDE 22 «No Reachable Authority»** (`At delegation xn--62-6kceem3eacgpr.xn--p1ai`) — его резолверы не могут достучаться до NS jino. Из России все четыре сервера (`217.107.34.200`, `195.161.62.86`, `217.107.219.170`, `81.177.139.205`) отвечают за 90–105 мс и корректно отдают A на GitHub Pages, Яндекс и Cloudflare резолвят. То есть дело в сетевой достижимости NS, а не в зоне. Ущерб нарастает третий замер подряд: **трафик из Google 9 → 5 → 3 визита**, пока Яндекс вырос с 26 до 35. Не устранено — см. пункт 4.1, это первоочередное.

**DNSSEC подключён у jino и доведён до конца (31.08).** Зона подписана (`DNSKEY 257 3 13`, ECDSA P‑256), DS опубликован в `.рф` (`1224 13 2 A2F049DC…4650F791`), дайджест сверен с ключом вручную — сходится; Cloudflare и Яндекс отдают ответ с флагом `ad`, то есть цепочка доверия валидируется. На `DNS_ERROR` это, как и ожидалось, не повлияло — подписи никогда не были причиной. Два следствия. Первое: подписи истекают **10.09.2026**, и теперь просроченная пересигновка у jino уронит домен уже у всех валидирующих резолверов, включая Яндекс, — проверить `RRSIG` числа 8–9 сентября. Второе: перед сменой NS DNSSEC обязательно выключить у jino и дождаться, пока DS уйдёт из `.рф`, иначе домен ляжет целиком — см. порядок в п. 4.1.

**Сделано и выкачено:** канонические адреса со слэшем на всём сайте (цепочка «canonical → редирект» разорвана) · `lastmod` в sitemap считается из git по каждой странице вместо времени сборки · цели в Метрике на отправку заявок с разбивкой по формам · редиректы `/about` и `/contacts` · `/dkp` возвращён в навигацию · страница ДКП приведена к стилю сайта, бланк с пунктом 3.5 · alt у картинок в отзывах · `.DS_Store` в `.gitignore`.

**Большая иконка сайта закрыта.** `icon-192.png` и `icon-512.png` собраны из логотипа и подключены в [nuxt.config.ts](nuxt.config.ts). После выката рекомендация `BIG_FAVICON_ABSENT` снялась, и в сводке Вебмастера впервые стоит **«Проблемы сайта: нет»**.

**Редиректы проверены на живом сайте** и работают лучше, чем ожидалось: `/about` отдаёт настоящий 301 (это GitHub Pages переводит на форму со слэшем), а заглушка на `/about/` содержит корректный `canonical` на `/o-nas/`. Для поисковика цепочка честная. В Метрике `/about` и `/contacts` пропали из страниц входа — поток со 100% отказов исчез.

**Цены поставлены на трёх страницах услуг** (пункт 2.1 закрыт частично): `/import/` — 150 000 ₽, `/proverka/` и `/registraciya-gibdd/` — по 4 000 ₽. Блок вынесен в переиспользуемый компонент [ServicePrice.vue](app/components/ServicePrice.vue), цена дублируется в разметку `Offer` и в мета‑описание страницы. Похоже, это и дало падение отказов с 31,6 до 22,8%.

**На главной появилась кнопка перехода на страницу импорта** — блок про импорт был тупиком, внутренний вес с самого сильного блока никуда не шёл.

---

## Где мы сейчас (база для замера прогресса)

| Показатель | 27.07 (база) | 30.08 | 01.09 |
|---|---|---|---|
| ИКС (SQI) | 0 | 0 | **0** |
| Проблемы сайта в Вебмастере | 1 рекомендация | FATAL: DNS_ERROR, 7 дней | **FATAL: DNS_ERROR**, 9 дней |
| Страниц в поиске | 10 | 10 | **10** (стабильно с 17.08) |
| Показы / клики в Яндексе (30 дн) | ~85 / 8 | 267 / 35 | **236 / 31**, CTR 13,1% |
| Визиты / посетители (Метрика, 30 дн) | 32 / 28 | 79 / 59 | **79 / 60** |
| Из поиска | 18 | 48 (Я 32, G 5) | **49** (Яндекс 35, **Google 3**) |
| Визиты из Рязани | 2 | 22 | **22** из 79 — 1‑е место, но лишь 28% |
| Отказы | — | 22,8% | **19,0%** (падают шестой замер подряд) |
| Глубина просмотра | — | — | **1,27 стр/визит**, 1 м 15 с |
| Входы на страницы услуг | ≈1 из 32 | 17 из 79 | **13 из 79** (главная 59, `/podbor/` 9, `/import/` 4) |
| Визиты с Яндекс.Карт | 1 | 0 | **0** |
| Отзывы на Авито / на Картах | 136 / 26 | без изменений | не проверялось |

**Позиции по коммерческим запросам:**

| Запрос | 27.07 (база) | 30.08 | 01.09 | Показы | Клики | CTR |
|---|---|---|---|---|---|---|
| автоподбор 62 рязань *(бренд)* | 4,2 | 3,2 | **3,2** | 18 | 6 | 33% |
| автоподбор 62 *(бренд)* | 3,3 | 3,2 | **3,1** | 28 | 6 | 21% |
| автоподбор рязань | 18,5 | 9,4 | **9,2** | 52 | 3 | **5,8%** |
| автоподбор в рязани | 15,6 | 8,8 | **8,7** | 23 | 1 | **4,3%** |
| подбор авто рязань | 17,0 | 8,6 | **8,7** | 11 | 2 | 18% |
| автоподбор рязань для покупки автомобиля | — | 8,1 | **8,1** | 11 | 1 | 9% |
| пригон авто в рязани | — | 4,5 | **4,5** | 2 | 1 | 50% |
| автоподборщик рязань | 19,0 | 8,5 | нет показов | 0 | 0 | — |

**Оговорка о замере 01.09:** он снят через два дня после предыдущего, окна в 30 дней перекрываются почти полностью. Разницу в показах и кликах (267/35 → 236/31) читать как шум и сдвиг окна, а не как падение. Значимы здесь не дельты, а то, что видно в структуре.

**Позиции встали на плато.** За две недели ни один коммерческий запрос не сдвинулся дальше десятых доли: «автоподбор рязань» 9,4 → 9,2, «автоподбор в рязани» 8,8 → 8,7, «подбор авто рязань» 8,6 → 8,7. Это ожидаемо и важно: техническая часть выжата, дальше без внешних сигналов не двинемся.

**Главная дыра в цифрах — CTR на коммерческих запросах.** «Автоподбор рязань»: 52 показа, 3 клика, **5,8%**. «Автоподбор в рязани»: 23 показа, 1 клик, **4,3%**. Для сравнения по бренду — 21–33%. То есть нас показывают, но не выбирают: девятая позиция это низ первой страницы, где выше стоят колдунщик Карт и конкуренты с ценами в сниппете. Один запрос даёт 52 показа — из них при нормальном CTR выходило бы 8–10 кликов вместо трёх. Это самая дешёвая недобранная выгода во всём плане, и упирается она в два пункта: цена в сниппете (п. 2.1, разметка `Product`) и карточка на Картах (приоритет 1).

**Люди заходят на главную и уходят с неё.** 79 визитов дали 100 просмотров, глубина 1,27 стр/визит. Отказы при этом упали до 19,0% — значит страницу читают, но следующего шага не делают. 59 входов из 79 — главная; страницы услуг почти не собирают трафик сами (`/podbor/` 9, `/import/` 4, остальные ноль). Прямое подтверждение п. 2.2: они слишком тонкие, чтобы ранжироваться самостоятельно.

**Из Рязани только 28% трафика.** Москва 13, Питер 10, Тула 5, Тверь 3 — 31 нецелевой визит, плюс Прага, Амстердам и Нью‑Йорк по 1–2 (боты или VPN). Для локального бизнеса это разбавляет поведенческие факторы в глазах Яндекса.

**Спрос, который видно в запросах и который мы не обслуживаем.** Люди ищут отзывы и адрес — «autopodbor_62 ул. грибоедова, 8б отзывы» в пяти вариантах написания, «автоподбор 62 рязань отзывы»: суммарно около 11 показов, кликов почти нет. Это тот самый приоритет 1, только теперь он подтверждён спросом, а не рассуждением: спрос на отзывы есть, а 136 отзывов лежат на Авито. Отдельно виден спрос на импорт («пригон авто из кореи в городе рязань», «таможенное оформление авто рязань») при четырёх входах на `/import/` и на юридические услуги («платные услуги купли продажи машины в рязани», «помощь при регистрации автомобиля в гибдд») — при этом `/dkp/` уже ловит 3 входа с нулевыми отказами и остаётся недооценённым активом.

**Три перекоса, которые видно в цифрах.** Первый: **Google осыпается третий замер подряд** — 9 → 5 → 3 визита, при живом и растущем Яндексе. Это единственная метрика, которая ухудшается, причина известна и не устранена. Второй: **ИКС по‑прежнему 0, с Карт ноль визитов** — позиции 8–9 взяты техникой и ценами, последние шаги до топ‑3 без внешних сигналов не даются. Третий: **показы есть, кликов нет** — работа над сниппетом окупится быстрее, чем работа над позициями.

---

## ПРИОРИТЕТ 1. Снять потолок: ИКС = 0

Ноль означает, что у Яндекса нет ни ссылочных, ни поведенческих сигналов о компании. Пока так — последние позиции до топ‑10 не взять, сколько бы ни правили страницы. Кода тут нет, работа руками. **Это единственный блок, который не сдвинулся ни на шаг с мая.**

Карточка в Яндекс Бизнесе заведена, регион присвоен (`NOT_IN_SPRAV` и `NO_REGIONS` в диагностике отсутствуют). Но как источник трафика она мертва: **0 визитов с Карт за 30 дней**, при том что по «автоподбор рязань» колдунщик Карт стоит выше органики.

- ☐ **Перенаправить поток отзывов с Авито на Яндекс.Карты — самое дешёвое действие во всём плане.** На Авито 136 отзывов, все с оценкой 5, последний от 14.03.2026. На Картах 26. Поток отзывов уже есть, он просто идёт туда, где не влияет на ранжирование в Рязани. Ничего создавать не нужно — поменять адрес просьбы: QR‑код при выдаче автомобиля, короткая ссылка в чате после сделки, в подписи. Цель — +15–20 отзывов за квартал.
- ☐ **Свежесть важнее количества.** Яндекс учитывает давность: мёртвая лента тянет карточку вниз сильнее, чем скромное число отзывов.
- ☐ **Аудит заполнения карточки** — фото офиса и работ, полный перечень услуг с ценами (те же, что появятся по п. 2.1), режим работы, категории.
- ☐ **Активность в карточке** — посты, обновление фото, ответы на все отзывы.
- ☐ **2ГИС и Google Business Profile** — завести и заполнить.
- ☐ **Ссылки на сайт** из профиля Авито, Telegram‑канала, локальных каталогов Рязани, профильных площадок автоподборщиков.
- ☐ **NAP‑консистентность**: одинаковые название, адрес и телефон везде. Заодно проверить e‑mail `info@autopodbor62.ru` в [schema.ts:32](app/utils/schema.ts#L32) — домен не совпадает с сайтом, убедиться, что почта живая.

---

## ПРИОРИТЕТ 2. Закрепиться в топ‑10 и поднять CTR

### ☐ 2.1 Цены на оставшихся страницах услуг

**Сделано:** `/import/` 150 000 ₽, `/proverka/` 4 000 ₽, `/registraciya-gibdd/` 4 000 ₽. Каждая цена стоит видимым текстом, продублирована в `Offer` у `Service` и у `Product` в JSON‑LD и вынесена в мета‑описание, чтобы попадать в сниппет. Компонент [ServicePrice.vue](app/components/ServicePrice.vue) — добавить цену на новую страницу теперь это один вызов.

- ☐ Цены на `/podbor/`, `/vykup/`, `/proverka-kuzova/` — нужны суммы от заказчика.
- ☐ Сводная таблица цен по услугам на главной.

**Разметка `Product` для цены в сниппете сделана (02.09).** Оговорка была такая: по [справке](https://yandex.ru/support/webmaster/ru/supported-schemas/goods-prices.md) Яндекс берёт цену в сниппет только из `Product`/`OfferCatalog`/`AggregateOffer`, а `Service` с `offers` читает как коммерческий фактор ранжирования, но в выдаче не показывает. Теперь на трёх страницах с ценой отдаются оба узла: `Service` описывает услугу по смыслу, `Product` существует ради цены в сниппете, `@id` у них разные — это не дубликат одной сущности. Хелпер `productJsonLd()` в [schema.ts](app/utils/schema.ts), обязательный набор полей Яндекса закрыт целиком (`name`, `description`, `brand`, `image` + `price`, `priceCurrency`, `availability` в `Offer`). Проверено на отрендеренном HTML: на `/import/`, `/proverka/` и `/registraciya-gibdd/` узел присутствует, поля на месте, цена в `Service` и `Product` совпадает. `aggregateRating` в `Product` намеренно не заведён: 136 отзывов с Авито относятся к компании, а не к отдельной услуге.

Заодно на `/registraciya-gibdd/` цена добавлена в мета‑описание — на двух других страницах она там была, а здесь нет; теперь описание 135 символов и начинается с «Регистрация автомобиля в ГИБДД в Рязани — 4 000 ₽».

Что осталось по этому пункту — дождаться переобхода и посмотреть в Вебмастере, собрался ли товарный сниппет, и подтянулся ли CTR по «автоподбор рязань» с текущих 5,8%.

Ориентир по конкурентам: [DSS Group](https://dss-g.ru/avtopodbor_ryazan/) — от 8 600 ₽ за диагностику и от 38 400 ₽ за подбор под ключ, [Автопарадигма](https://avtoparadigma.ru/ryazan/) — таблица от 30 000 до 64 000 ₽ в зависимости от бюджета покупки.

### ☐ 2.2 Нарастить объём текста

Замер по отрендеренному HTML (страницы с ценами пересняты 30.08, остальные — 15.08):

| Страница | Слов |
|---|---|
| `/o-nas/` | 229 |
| `/podbor/` | 376 |
| `/proverka/` | 399 |
| `/registraciya-gibdd/` | 484 |
| `/vykup/` | 446 |
| `/import/` | 577 |
| `/proverka-kuzova/` | 479 |
| `/` | 919 |

У конкурентов в топ‑10 Рязани: Автопарадигма 3 500–4 000 слов, DSS Group 8 000–10 000. Разрыв в 10–25 раз.

Цель — 1 000–1 500 слов на страницу услуги. Чем добивать: развёрнутый процесс работы, что входит и что не входит, частые проблемы и как их ловим, примеры из практики с фото, сравнение вариантов услуги.

### ☐ 2.3 Кейсы с фото и ценами

Есть у обоих конкурентов, у нас нет. У Автопарадигмы восемь кейсов с ценой до и после торга — очень сильный формат: одновременно доверие, объём текста и коммерческие сигналы.

### ☐ 2.4 FAQ на каждую страницу услуги

`faqPageJsonLd()` используется только на главной.

- Раскомментировать два вопроса на главной — [index.vue:843](app/pages/index.vue#L843) — и добавить их в `faqPageJsonLd()`.
- По 3–5 вопросов на каждую страницу услуги + разметка. У Автопарадигмы 9 вопросов, у DSS — 4.

### ☐ 2.5 Вывести больше отзывов Авито в HTML

Сейчас на главной **6 карточек**, из них 5 про проверку автомобиля и 1 про эндоскопию двигателя. На Авито 136. В отличие от виджета Карт (iframe, в индекс не идёт), эти отзывы лежат прямо в HTML: живой текст с упоминанием услуг, марок и Рязани. Самый дешёвый способ добрать объём по п. 2.2 — писать ничего не надо.

- Довести до 10–12 карточек, остальные под кнопку «Показать ещё» (рендерить в HTML сразу, а не подгружать по клику).
- Брать отзывы с разными услугами: в выгрузке есть регистрация в ГИБДД и чистка номера двигателя — они дадут вхождения для соответствующих страниц.
- Разметить как `Review` внутри `LocalBusiness`, тогда `aggregateRating` перестанет быть висящим числом.

Про ожидания: для `LocalBusiness` Google считает отзывы, размещённые самим бизнесом у себя на сайте, self‑serving и в расширенные сниппеты их не выводит. Рассчитывать стоит на объём текста, поведенческие и Яндекс — но не на звёзды в гугловой выдаче.

### ☐ 2.6 Отдельная страница «Контакты»

Для локального бизнеса — одна из главных посадочных. Спрос подтверждён: люди угадывают адрес `/contacts` и попадают на редирект, плюс в Вебмастере есть запрос «автоподбор рязань контакты сайт». Сейчас роль тянет `/o-nas/` на 229 словах.

Нужны: карта, схема проезда, режим работы, телефон, мессенджеры, фото офиса на Грибоедова 8Б. После запуска — переставить редирект `/contacts` с `/o-nas/#kontakty` на новую страницу ([nuxt.config.ts](nuxt.config.ts), константа `REDIRECTS`).

---

## ПРИОРИТЕТ 3. Импортный кластер — главная ставка

Направление, где мы **уже в топ‑10**, а конкуренция слабая. Топ по «автоподбор рязань» держат федеральные сети с региональными посадочными ([DSS Group](https://dss-g.ru/avtopodbor_ryazan/), [Автопарадигма](https://avtoparadigma.ru/ryazan/), [авто‑подбор.рф](https://xn----7sbecl2dbcfoo.xn--p1ai/ryazan-62/), [THE Автоподбор](https://the-autopodbor.ru/ryazan)) — у них накопленный вес домена и сетка на 39 городов, это забег на год‑полтора. А связку «Рязань + Корея/Китай» локально почти никто не занимает.

Текущие позиции:

| Запрос | Позиция на 30.08 | Клики |
|---|---|---|
| автоподбор 62 машины из китая | 1,0 | 1 |
| привезти авто из за рубежа рязань | 4,0 | 0 |
| пригон авто в рязани | 4,5 | 1 |
| импорт машин рязань | 5,7 | 0 |
| пригон авто из Кореи под ключ рязань | 7,0 | 1 |
| пригнать авто рязань | 7,5 | 0 |
| пригнать машину рязань | 8,0 | 0 |
| пригон авто из Кореи в городе Рязань | 9,5 | 0 |

По показам импортные запросы уже сопоставимы с «автоподборными», но по позициям мы там в топ‑10, а не на второй странице.

- ☐ Отдельные страницы под Корею и под Китай.
- ☐ Калькулятор пошлин.
- ☐ Сроки и этапы доставки.
- ☐ Галерея пригнанных авто с ценами.

При этом `/import/` — вторая по посещаемости страница сайта, а в ней 454 слова.

---

## ПРИОРИТЕТ 4. Инфраструктура

### ☐ 4.1 Увести DNS от jino и переехать с GitHub Pages

Два риска, которые в августе выстрелили оба.

**DNS — самое срочное в плане.** Сбой случился дважды: 11–12 августа (SERVFAIL по всей зоне jino, включая сам `jino.ru` и его NS) и **снова 19 августа**, где Google отвечает прямо: «Name servers did not respond [ns3.jino.ru]». Cloudflare и Яндекс при этом резолвят нормально, сайт жив.

Последствия: посетители на Google DNS (частая настройка по умолчанию, в том числе на Android и роутерах) сайт не открывают, а робот Google не может его обойти — Google сейчас даёт 9 визитов и держит нас в индексе, но это ровно тот сценарий, по которому в августе мы уже потеряли главную в Яндексе.

Повторяемость доказана, ждать бессмысленно — переносить зону.

Куда: NS регистратора (RU‑CENTER), Cloudflare или Яндекс DNS. Зона снята с `ns1.jino.ru` 01.09, переносить нужно ровно это:
- **A** (apex) — `185.199.108.153`, `.109.153`, `.110.153`, `.111.153`
- **www** — CNAME на `temalev.github.io`
- **TXT** — обе обязательно, иначе отвалится доступ в Вебмастер и Search Console:
  `google-site-verification=IVmtJ63YY0gqUy4Dy0qa48x6sNwu9UjjGv2LcPcwdpc`
  `yandex-verification: 714fa8559263ac0d`
- **CAA** — `0 issue "letsencrypt.org"`, нужна для выпуска сертификата GitHub Pages
- **MX, AAAA** — отсутствуют, почты на домене нет

**Порядок переезда обязателен — с 31.08 на домене живой DNSSEC.** Если сменить NS, не сняв DS, резолверы станут проверять подписи ключом jino на серверах нового провайдера, который их не подписывал: SERVFAIL у всех валидирующих резолверов, включая Яндекс, — то есть домен ляжет целиком, а не только для Google. Правильная последовательность:
1. Выключить DNSSEC в панели jino и дождаться, пока DS уйдёт из `.рф` — проверяется `dig +norec @a.dns.ripn.net xn--62-6kceem3eacgpr.xn--p1ai DS`, должно стать `ANSWER: 0`. С учётом TTL 3600 — час‑два.
2. Только после этого менять NS.
3. Когда зона поднялась у нового провайдера — включить DNSSEC уже там (у Cloudflare подпись и пересигновка автоматические, DS отдаётся сам).

Прокси Cloudflare (оранжевое облако) не включать — только DNS, иначе можно сломать выпуск сертификата GitHub Pages.

**Хостинг.** Сайт отдаётся GitHub Pages через франкфуртский edge (`x-served-by: cache-fra`), TTFB скачет от 0,3 до 6 секунд. Именно провал в доставке чанка стоил нам главной в индексе. Плюс на GitHub Pages невозможны настоящие 301 — редиректы работают через мета‑обновление.

Куда: Timeweb / Selectel / VK Cloud + российский CDN. Билд статический, переезд — перенос `.output/public` и правка [deploy.yml](.github/workflows/deploy.yml) на деплой по SSH/rsync. Бонусы: настоящие 301 (правила в `routeRules` заработают как положено без изменений кода) и кэш‑заголовки (сейчас `max-age=600` на всё, включая хэшированные ассеты `_nuxt/*`, которым можно ставить год).

### ☐ 4.2 Self‑host шрифтов

Четыре обращения к `fonts.googleapis.com` / `fonts.gstatic.com` в [nuxt.config.ts](nuxt.config.ts) — render‑blocking на критическом пути, домен в РФ работает нестабильно. Локально сейчас лежит только `Arial.ttf` (он для генерации PDF, не для сайта).

Скачать Inter (веса 400/500/600/700/800) и Material Symbols в `public/fonts/`, подключить через `@font-face` с `font-display: swap`, убрать `preconnect` и оба `<link rel="stylesheet">`.

### ☐ 4.3 Картинки услуг в WebP + ресайз логотипа

Висит с мая, остаётся главным замечанием по Core Web Vitals.

| Файл | Размер | Вес |
|---|---|---|
| `public/images/services/1.jpeg` | 1024×1024 | **784 КБ** |
| `public/images/services/2.jpeg` | 1024×1024 | **704 КБ** |
| `public/images/services/3.jpeg` | 1024×1024 | **353 КБ** |
| `public/images/logo.png` | **1684×1684** | 125 КБ (в шапке 44×44) |

1. Конвертировать:
   ```bash
   cd public/images/services
   for f in *.jpeg; do cwebp -q 82 "$f" -o "${f%.jpeg}.webp"; done
   rm *.jpeg
   ```
2. Заменить `.jpeg` → `.webp` в коде (в каждом файле два места — `PAGE_OG_IMAGE` и hero `<img>`): [podbor.vue](app/pages/podbor.vue), [proverka.vue](app/pages/proverka.vue), [vykup.vue](app/pages/vykup.vue), [proverka-kuzova.vue](app/pages/proverka-kuzova.vue) (там `3.jpeg` для OG и `2.jpeg` для hero), [registraciya-gibdd.vue](app/pages/registraciya-gibdd.vue).
3. Ресайзить `logo.png` до 256×256 (или сделать SVG). Он же идёт в JSON‑LD как `logo`/`image` — [schema.ts:29](app/utils/schema.ts#L29).
4. Проверить: `npx nuxt generate`, DevTools → Network → Img.

### ☐ 4.4 Мелочи одним коммитом

Статусы проверены 15.08:

- Второй `<h1>` на `/dkp` — в файле два `<h1>`, второй в печатном макете (скрыт стилями, но в DOM). Сделать `<h2>` или `<div>`.
- `og:image` + `twitter:image` на `/privacy-policy` — отсутствуют, подставить `${SITE_URL}/images/bg.webp`.
- `image` не передаётся в `serviceJsonLd()` на главной — [index.vue:38](app/pages/index.vue#L38), подставляется квадратный логотип 1684×1684. Передать `bg.webp`.
- Глобальный `{ name: 'robots', content: 'index, follow' }` — на проде meta robots нет вообще.
- `{ name: 'theme-color', content: '#0a0a0a' }` и `{ name: 'format-detection', content: 'telephone=no' }`.
- `Clean-param` в [public/robots.txt](public/robots.txt) — иначе Яндекс считает `/?utm_source=…` отдельной страницей:
  ```
  Clean-param: utm_source&utm_medium&utm_campaign&utm_term&utm_content&yclid&gclid&fbclid&_openstat&from
  ```
- Урезать title (Google режет ~60 символов): `/dkp/` — 85, `/vykup/` — 85, `/` — 80. Убрать хвост `| Автоподбор 62`.
- Урезать description до 150–160: `/` — 219, `/dkp/` — 184, `/registraciya-gibdd/` — 176, `/o-nas/` — 173, `/podbor/` — 165, `/vykup/` — 161.
- JSON‑LD `WebSite` в [app/app.vue](app/app.vue) рядом с `LocalBusiness` — для @id‑связок. Сейчас отсутствует.
- Удалить мёртвую зависимость `@element-plus/icons-vue` из [package.json](package.json) — в коде не используется.

---

## ПРИОРИТЕТ 5. Рост на 3–6 месяцев

### ☐ 5.1 Блог / раздел «Полезное»

Главный способ поднять ИКС и собрать информационный трафик. `/blog/` или `/poleznoe/`, 2–4 материала в месяц по 3–5 тысяч знаков, в каждом CTA на услугу.

Темы под реальный спрос из Вебмастера: «как проверить авто перед покупкой», «признаки скрученного пробега», «постановка на учёт в 2026», «пошлины на авто из Китая в 2026», «заказать диагностику автомобиля перед покупкой» (запрос уже даёт показы на 8,5 позиции), разборы кейсов с фото.

### ☐ 5.2 Отзывы на страницах услуг

Два разных инструмента, не путать:

- **Виджет Карт** ([YandexReviews.vue](app/components/YandexReviews.vue)) стоит на главной, компонент переиспользуемый. Контент в iframe — в индекс не идёт, объём текста не растит, работает на доверие. Вешать на страницы услуг не раньше, чем там появятся цены и текст: шесть тяжёлых iframe — заметная нагрузка ради блока, который поисковик не увидит.
- **Карточки отзывов с Авито** — обычный HTML, индексируются. Их и надо разносить по страницам услуг, фильтруя по `itemTitle`.

### ☐ 5.3 Региональные посадочные

`/podbor-skopin/`, `/podbor-kasimov/` и т.п. — **только после того, как Рязань выйдет в топ‑10**. Раньше смысла нет.

---

## Порядок работ

1. **Увести DNS от jino (п. 4.1)** — сбой уже повторился, он режет посетителей и обход Google прямо сейчас. Единственный пункт, где ущерб идёт непрерывно.
2. **Отзывы на Карты (п. 1)** — не требует кода и разработчика, запускается сегодня, эффект накопительный. Параллельно со всем остальным.
3. Цены (п. 2.1) — вечер работы, снимает ноль кликов по ценовым запросам.
4. Один технический коммит: мелочи из п. 4.4.
5. Импортный кластер (п. 3) — основная ставка на квартал.
6. Объём текста, FAQ, кейсы, отзывы в HTML (пп. 2.2–2.5).
7. Второй технический коммит: 4.2 + 4.3 (скорость).
8. Переезд с GitHub Pages (п. 4.1).
9. Блог (5.1) — регулярно, начиная как можно раньше.

## Контрольные показатели

Замеряем в середине октября против колонки 30.08 вверху файла:

- «автоподбор рязань», «автоподбор в рязани» — из 8,8–9,4 **устойчиво в топ‑5**.
- Удержать в топ‑10 «подбор авто рязань» и «автоподборщик рязань» (позиции свежие, могут откатиться).
- ИКС — с 0 до **≥10**.
- Визиты из Рязани — с 22 до **40+** в месяц.
- Входы на страницы услуг — с 17 из 79 до **30%** от всех визитов.
- Клики из Яндекса — с 35 до **60+** в месяц.
- Трафик из Google — вернуть с 5 хотя бы до **15+** (упирается в DNS).
- Отзывы на Яндекс.Картах — с 26 до **45+**, с отзывами текущего месяца в ленте.
- Визиты с Яндекс.Карт — с 0 до **15+** в месяц (в Метрике источник «Yandex.Maps»).
- Заявки — считать по цели «Заявка отправлена (форма сайта)», разбивка по формам в параметрах визитов.
- Домен резолвится через Google DNS **без сбоев в течение месяца** (проверять `https://dns.google/resolve?name=xn--62-6kceem3eacgpr.xn--p1ai&type=A`, Status должен быть 0).

Инструменты замера:
```bash
node ~/.claude/skills/yandex-webmaster/webmaster.mjs --report all --days 30
node ~/.claude/skills/yandex-metrika/metrika.mjs --report all --days 30
```

## Что прогонять после каждого деплоя

- https://pagespeed.web.dev/ — Core Web Vitals на главной и одной услуге (после 4.2 и 4.3 LCP должен заметно упасть).
- https://webmaster.yandex.ru/tools/microtest/ и https://search.google.com/test/rich-results — валидность JSON‑LD.
- Переобход в Вебмастере по списку из [routes-for-webmaster.txt](routes-for-webmaster.txt). Квота 150 URL в день.
- Раз в неделю — отчёт «Запросы» в Вебмастере: где выросли позиции, где показы есть, а кликов нет (значит, переписать title/description).
- **Раз в неделю — «Страниц в поиске» в Вебмастере.** После августовского выпадения это ранний индикатор: падение числа страниц означает, что робот увидел ошибку.
